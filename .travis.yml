language: php

php:
  - 7.0
  - 7.1
  - 7.2

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --prefer-lowest --no-interaction --prefer-dist

script:
  - ./vendor/bin/phpunit --coverage-text
